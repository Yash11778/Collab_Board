{
  "version": 2,
  "buildCommand": "npm install && cd hackathon && npm install && npm run build",
  "installCommand": "npm install && cd hackathon && npm install",
  "outputDirectory": "hackathon/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
